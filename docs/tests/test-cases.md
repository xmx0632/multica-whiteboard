# 教学白板「数学方程智能出图」测试用例

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 配套策略 | `docs/tests/test-strategy.md`（分层/环境/准入准出见彼处） |
| 被测版本 | `feat/equation-element-integration` @ `65bb8b2` |
| 用例规模 | 11 组共 114 条：P0 52 · P1 44 · P2 18 |

**表头约定**

- **优先级**：P0 阻断发布 / P1 发布前应过 / P2 可延后。
- **分层**：L1 单测（vitest·node）/ L2 组件（vitest+RTL+jsdom）/ L3 E2E（Playwright）/ Perf 性能 / Man 手工。
- **现状**：`已覆盖→文件` 表示已有自动化直接覆盖（回归保留）；`待补` 为本策略建议新增。
- **通用前置**（除特别说明）：桌面 Chrome ≥1920×1080，`npm run build && npm start` 起服务，打开主路由 `/`，白板为空文档。

---

## A. 方程输入与编辑器（14 条）

前置：`F` 或点工具栏 ƒ 进入 equation 工具，右侧出现方程编辑面板（264px，含输入框/状态行/预览/符号/模板/插入按钮）。

| ID | P | 用例（步骤 → 预期） | 分层 | 现状 |
|---|---|---|---|---|
| A-01 | P0 | 点工具栏 ƒ / 按 `F` → 激活 equation 工具（ƒ 按钮高亮），属性面板切换为方程编辑器且输入框自动聚焦 | L3 | 待补 |
| A-02 | P0 | equation 工具下在画布任意处按下拖动松开 → 不产生任何元素（画布不落笔），仅右侧面板承接输入 | L3 | 待补 |
| A-03 | P0 | 输入 `y=sin(x)` → 状态行实时变绿「✓ 已识别：显式函数 y=f(x)」；输入 `(x-1)²+(y-2)²=9` → 「✓ 已识别：圆（几何方程）」 | L2 | 待补 |
| A-04 | P0 | 输入 `y=foo(x)` → 状态行红「⚠ 无法识别的符号 "foo"」且输入框红边框；随输入实时刷新 | L2 | 待补 |
| A-05 | P0 | 输入框为空/纯空白 → 状态行灰「等待输入…」，「插入图形 ⏎」按钮 disabled | L2 | 待补 |
| A-06 | P0 | 输入合法方程后按回车 → 触发确认（与点「插入图形」等价，payload 含 equation+outcome）；空输入回车 → 仅聚焦不确认 | L2 | 待补 |
| A-07 | P1 | 按 `Escape` → 输入框失焦，不确认、不清空、不切工具 | L2 | 待补 |
| A-08 | P1 | 光标置于 `y=sin(x)` 的 `sin` 前，点符号按钮 `²` → 在光标处插入为 `y=²sin(x)`（光标停在插入文本之后，焦点保持输入框） | L2 | 待补 |
| A-09 | P0 | 依次点 13 个模板 → 输入框整体替换为模板方程、焦点回输入框且光标在末尾、状态行/预览同步刷新为该模板结果 | L2 | 待补 |
| A-10 | P1 | 点「清空」→ 输入框清空并聚焦，状态行回「等待输入…」，插入按钮回 disabled | L2 | 待补 |
| A-11 | P1 | MiniPreview 实时预览：合法显式函数 → 240×96 预览出曲线；圆/椭圆 → 出几何图形；非法 → 预览区显示错误文案不出曲线 | L2 | 待补（预览数据管线 L1 已覆盖→`sample.test.ts` createPreviewPolylines） |
| A-12 | P1 | 输入非法方程（如 `y=#1`）按回车 → 确认**仍被允许**，payload.outcome.kind='error'（供生成错误占位元素，原型决策 4） | L2 | 待补 |
| A-13 | P1 | 输入框聚焦时按 `V`/`B`/`R` 等工具快捷键 → **不切换工具**（快捷键处理器跳过 INPUT/TEXTAREA 焦点） | L3 | 待补 |
| A-14 | P2 | 编辑器 autoFocus；从错误占位「重新编辑方程」进入 → 输入框预填原方程（见 E-02） | L2 | 待补（E-02 在 L3 覆盖） |

## B. 归一化与解析（13 条）

前置：可直接单测调用 `normalizeEquation` / `parseEquation` / `validateEquation`。

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| B-01 | P0 | PRD §5.2 **P0 方程族**全部解析成功且求值正确：`y=2x+1`、`y=x²-2x-3`、`y=x³-2x`、`y=sin(x)`、`y=2sin(2x+π/3)`、`y=tan(x)`、`y=√x`、`y=1/x`（关键点数值抽查，如 x=0.5 处 sin 值） | L1 | 已覆盖→`parse.test.ts`（13 模板 + P0/P1 求值） |
| B-02 | P0 | **P1 方程族**：`y=2ˣ`、`y=eˣ`、`y=ln(x)`、`y=|x-1|`、log 带底 `y=log(2,x)`、反三角 `asin/acos/atan` 求值正确 | L1 | 已覆盖→`parse.test.ts` |
| B-03 | P0 | 圆 `(x-1)²+(y-2)²=9` → circle{cx:1,cy:2,r:3}；椭圆 `x²/9+y²/4=1` → ellipse{rx:3,ry:2}（含带偏移 `(x+2)²` 负心坐标） | L1 | 已覆盖→`parse.test.ts` |
| B-04 | P0 | **非标准形几何**（展开形 `x²+y²+2x=1`、交叉项 `xy=1`、`x²=2py`）→ 走隐式方程拒绝文案，不误判为圆/椭圆、不崩溃 | L1 | 待补（部分由隐式拒绝用例间接覆盖） |
| B-05 | P0 | `y=` 前缀可省略（`sin(x)` ≡ `y=sin(x)`）；`f(x)=` 前缀剥离 | L1 | 已覆盖→`parse.test.ts` |
| B-06 | P0 | Unicode 输入面归一：π→pi、²³⁴/ˣ→^n、√(…)→sqrt(…)（含嵌套）、|…|→abs(…)、·×→*、÷→/、全角括号/逗号、大写函数名转小写 | L1 | 已覆盖→`normalize.test.ts` |
| B-07 | P0 | 隐式乘法：`2x`、`2sin(2x+π/3)`、`2πx`/`2pix`→2·pi·x、`xsin(x)`→x*sin(x)；`asin` 贪心优先于 `sin` 不误拆 | L1 | 已覆盖→`normalize.test.ts` |
| B-08 | P0 | 五类错误文案逐条命中：空输入「请输入方程」/缺右侧「方程缺少右侧表达式」/未知符号「无法识别的符号 "…"」/未知字符（`#` 前置拦截）/括号未闭合「括号或绝对值符号未闭合」/表达式不完整 | L1 | 已覆盖→`parse.test.ts` |
| B-09 | P0 | 非多项式隐式方程拒绝：`sin(x)=cos(x)`、`x³+y=1` → 「暂不支持该隐式方程…」（二元一次/二次已支持：`x=y`→line、`x²-y²=1`→hyperbola，ZOO-146/147） | L1 | 已覆盖→`parse.test.ts` |
| B-10 | P1 | 椭圆分母 0/负 → 「椭圆参数必须为正」；圆 r=0（`x²+y²=0`）→ 退化单点 point（ZOO-148 行为升级，原报「半径必须为正」） | L1 | 已覆盖→`parse.test.ts` |
| B-11 | P2 | **超长/深嵌套表达式**（1000 字符、200 层括号、`x^x^x^…` 连续幂）→ 限时返回解析结果或明确错误，不栈溢出、不冻结 UI | L1 | 待补 |
| B-12 | P1 | 求值域外返回 NaN 不抛异常（`ln(x)` x<0、`1/x` x=0）；`validateEquation` 返回结构与编辑器契约一致（无 fn 泄漏） | L1 | 已覆盖→`parse.test.ts` / `parse.test.ts` validate 契约 |
| B-13 | P2 | 混合输入：`Y=SIN(X)+Π`（大写）与全角 `y＝` 等非常规写法的容错边界（小写归一已覆盖；`＝`全角等号为已知不支持点，验证给出可读错误而非崩溃） | L1 | 待补 |

## C. 采样与出图渲染（14 条）

前置：L1 直调 `sampleExplicit`/`sampleEquation`/`resolvePlotRender`；断笔用例检查折线分段。

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| C-01 | P0 | `y=tan(x)` 域 [-10,10] → 按渐近线断成多段，**无贯穿竖线** | L1 | 已覆盖→`sample.test.ts` |
| C-02 | P0 | `y=1/x` 域 [-10,10] → 断成两段，x=0 点不出现在折线 | L1 | 已覆盖→`sample.test.ts` |
| C-03 | P0 | `y=√x` / `y=ln(x)` → 负半轴无效点断笔、有效段完整保留 | L1 | 已覆盖→`sample.test.ts` |
| C-04 | P0 | 陡峭单调曲线 `y=x³` 大定义域**不误杀**（断笔需双侧跨越视窗，风险 R3） | L1 | 已覆盖→`sample.test.ts` |
| C-05 | P0 | 定义域校验：xmin≥xmax → 「定义域无效：xmin 需小于 xmax」；宽度 ∉[0.1,1000] → 「宽度需在 0.1–1000 之间」 | L1 | 已覆盖→`sample.test.ts` |
| C-06 | P0 | 采样数 clamp 到 [2,2000]（硬上限，UI 档位 160/320/640 之外的值不可达） | L1 | 已覆盖→`sample.test.ts` |
| C-07 | P1 | y 视窗四分位自适应：`y=eˣ` 域 [-10,10]（峰值≈22026）不撑爆视窗；常数函数 `y=5` 给最小视窗 | L1 | 已覆盖→`sample.test.ts` |
| C-08 | P0 | 圆/椭圆参数化精确路径：120 段闭合折线，首尾点重合，包围盒含 15%+0.5 padding | L1 | 已覆盖→`sample.test.ts` |
| C-09 | P0 | 三层坐标映射：数学 y 向上 → 局部 y 向下，视窗端点映射到卡片角；等比模式 y 视窗按宽高比推导（圆渲染为正圆不变形） | L1 | 已覆盖→`plot.test.ts` |
| C-10 | P0 | 刻度系统：普通域 niceStep（1/2/2.5/5×10^k）；π 域（端点近 π/2 整数倍）启用 π 刻度（`2π`、`3π/2` 分数标签）；普通数值去尾零 | L1 | 已覆盖→`plot.test.ts` |
| C-11 | P1 | 网格密度阈值：线距 <8px 时整层隐藏（不画亚像素网格） | L1 | 待补（`stepForAxis` 相关已覆盖，阈值分支待补） |
| C-12 | P1 | **轴出视窗**：定义域全正（如 [2,10]）时竖轴/横轴按可见性独立判定，不画界外轴、不报错 | L1 | 待补 |
| C-13 | P1 | 渲染缓存签名：sig 只含数学输入（方程/视窗/档位/尺寸）——平移/缩放/改颜色线宽透明度/轴网显隐**零重采样**（`plotRenderWriteCount` 差分）；改定义域/采样档/方程 → 恰好重建一次 | L1 | 已覆盖→`plot.test.ts` + `mathplot-integration.test.ts` |
| C-14 | P2 | 刻度标签防拥挤 stride（最小 40px 间距）在大/小定义域下不重叠溢出卡片 | L1/Man | 待补 |

## D. MathPlot 元素集成（20 条）

前置：主路由，已创建至少一个 mathPlot 元素。

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| D-01 | P0 | **创建主链路**（PRD 验收 1）：F → 输入 `y=sin(x)` → 回车 → 画布可视中心出现带坐标系+网格+曲线的卡片元素、自动选中（8 控点选中框）、工具自动切回选择（V） | L3 | 待补 |
| D-02 | P0 | 新元素默认参数：外框 480×360、定义域 [-10,10]、采样 320、轴/网格/标签 chip 全开、曲线色 #3B82F6、线宽 2、不透明度 100% | L1+L3 | 已覆盖→`mathplot-integration.test.ts`（工厂）；L3 抽查待补 |
| D-03 | P0 | 样式继承：工具栏先选红色/线宽 4 → 新建 mathPlot 元素继承该 strokeColor/strokeWidth | L1+L3 | 已覆盖→工厂用例；L3 待补 |
| D-04 | P1 | 先平移+缩放视口（如 scale=0.5）再创建 → 元素落在**当前可视区中心**（屏幕坐标换算正确） | L3 | 待补 |
| D-05 | P1 | 小可视区（窄窗口/高倍放大）创建 → 默认尺寸按比例收缩，不溢出屏幕 | L1 | 已覆盖→`mathplot-integration.test.ts`（工厂收缩） |
| D-06 | P0 | 选中：单击卡片 bbox 任意处命中 → 8 控点（4 角+4 边中点）选中框 + 属性面板切到「数学图形」参数面板；点空白 → 取消选中 | L3 | 待补 |
| D-07 | P0 | 移动：拖拽卡片 → 元素整体移动，坐标系/曲线/标签随动，数学内容（定义域/采样）不变 | L3 | 待补 |
| D-08 | P0 | 8 控点缩放：拖角/边控点 → 外框变化，最小 120×90 限制生效；等比模式下角控点锁定纵横比（圆仍为正圆） | L3 | 待补 |
| D-09 | P0 | **缩放语义**：缩放只改单位像素密度，xAxis/曲线形状不变（拉大后同一方程仍是同一图形，仅变大）——与「缩放=改视窗」的歧义回归 | L3 | 待补（L1 缓存签名已间接固化） |
| D-10 | P0 | 历史粒度（D5 两段式）：滑杆拖动全程实时预览但**一次拖拽只压一条**快照，Ctrl+Z 一次回到拖前；移动/缩放同理 | L1+L3 | 已覆盖→store transient 用例；L3 待补 |
| D-11 | P0 | 撤销重做全链路：创建/删除/移动/缩放/方程变更/参数变更各操作 Ctrl+Z 撤销、Ctrl+Shift+Z 重做，元素状态逐级恢复（含数学字段原位替换可撤销） | L3 | 待补（store 逻辑 L1 已覆盖） |
| D-12 | P0 | 属性面板调参实时重绘矩阵：定义域预设（-2π~2π/-5~5/-10~10 高亮态切换）与数字输入（step 0.5）、采样档（粗/中/细高亮）、坐标轴/网格/方程标签开关、等比开关（仅显式函数显示）、12 色板+自定义取色、线宽 1–8(step 0.5)、不透明度 20–100% —— 每项修改画布即时生效 | L3 | 待补 |
| D-13 | P1 | 面板内改方程文本 → blur 收敛重新分类：`y=sin(x)` 改 `y=x²-2x-3` → 徽标与图形切换；改成非法 → 元素转错误态 | L3 | 待补 |
| D-14 | P0 | 复制：面板「复制」→ 新元素 id 不同、位置 +24px 偏移、参数同源、自动选中新元素 | L3 | 待补 |
| D-15 | P0 | 删除：面板「删除」/ 选中后 `Del` → 元素移除且可 Ctrl+Z 恢复 | L3 | 待补 |
| D-16 | P1 | 几何方程面板形态：圆/椭圆选中时**隐藏**定义域与采样精度区、强制等比（无等比开关），显示「按参数化精确绘制，自动等比坐标」说明 | L2 | 待补 |
| D-17 | P1 | 多图形共存（PRD 场景 5）：sin（蓝）/抛物线/圆同屏 + 手绘批注 + 文本标注混排，互不干扰、各自可独立选中调参 | L3 | 待补 |
| D-18 | P1 | 平移/缩放全程帧内不重采样（缓存命中）——快速拖动视口时曲线无闪烁/掉帧跳变 | Perf | 待补（与 H-02 合并执行） |
| D-19 | P2 | 撤销栈深度 100 上限：连续 120 次操作后最早操作被挤出，栈行为与现有元素一致 | L3/Man | 待补 |
| D-20 | P1 | 历史面板：mathPlot 的创建/删除/参数变更在历史面板可见并可跳转（复用现有面板） | L3 | 待补 |

## E. 错误处理与稳健性（8 条）

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| E-01 | P0 | 确认非法方程（如 `y=#1`）→ 生成**红色虚线占位元素**（⚠ 原因 + 原方程 + 「点击重新编辑」提示），占位同为元素：可移动/删除/撤销（原型决策 4） | L3 | 待补 |
| E-02 | P0 | 原位重编辑：选中占位元素 → 面板「重新编辑方程」→ 编辑器预填原方程 → 修正为合法（如 `y=sin(x)`）确认 → **同一元素原位替换**（id/位置/样式保留），可 Ctrl+Z 回到错误态 | L3 | 待补 |
| E-03 | P0 | 五类错误端到端：空确认/未知符号/括号未闭合/表达式不完整/隐式不支持 → 编辑器实时拦截；强行确认后占位元素显示对应文案，全程不白屏 | L3 | 待补（文案 L1 已覆盖→`parse.test.ts`） |
| E-04 | P0 | 「定义域内无有效值」：`y=√x` 定义域改 [-5,-1]（全负无解）→ 元素转错误态显示该文案；改回 [-10,10] 恢复出图 | L3 | 待补（L1 已覆盖→`sample.test.ts`） |
| E-05 | P1 | 调参反向定义域：xmin 输 5、xmax 输 -5 → 元素转错误态（「xmin 需小于 xmax」）不崩溃；改回恢复 | L3 | 待补（L1 已覆盖） |
| E-06 | P1 | 定义域宽度越界：输入 [0,1001] 或 [0,0.05] → 错误态文案「宽度需在 0.1–1000 之间」 | L3 | 待补（L1 已覆盖） |
| E-07 | P0 | **模糊测试**：批量随机/边界字符串输入方程框（超长串、emoji、中文、换行粘贴、仅符号、重复括号 ≥500 组合）逐键校验与确认 → 无白屏、无崩溃、无无限循环，错误均有文案 | L1/L3 | 待补 |
| E-08 | P1 | 粘贴多行文本（含换行的多方程）→ 归一化去空白后整体解析：或给出可读错误，绝不静默画出错误图形 | L1 | 待补 |

## F. 持久化 / 导出 / 兼容（9 条）

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| F-01 | P0 | 保存 → 刷新重开（PRD 验收 4）：mathPlot 元素数量、方程、定义域、采样档、显隐开关、颜色/线宽/不透明度、位置尺寸全部还原，出图一致 | L3 | 待补 |
| F-02 | P0 | 错误占位元素持久化：error/kind 字段序列化往返，重开后仍是错误占位且可重编辑 | L3 | 待补 |
| F-03 | P1 | autosave：编辑后直接刷新 → 自动恢复最近状态（autosave 键） | L3 | 待补 |
| F-04 | P1 | 多文档链路：含 mathPlot 的文档走 localStorage + `/api/whiteboards` 双通道保存/列表/打开/删除，往返一致 | L3 | 待补 |
| F-05 | P1 | PNG 导出：含 mathPlot 的白板导出位图 → 图形完整非空白（像素抽检曲线色存在）、不报错 | L3 | 待补（`renderElement` 复用链路 L1 已覆盖） |
| F-06 | P1 | SVG 导出：曲线 path+网格+轴+标签 chip；错误态为虚线框+文案；显隐开关受控 | L1 | 已覆盖→`mathplot-integration.test.ts` |
| F-07 | P2 | 旧文档（迭代一产物，无 mathPlot）在新版本打开 → 全部元素正常，零影响 | L3 | 待补（L1 前向兼容已覆盖） |
| F-08 | P2 | 前向兼容：含 mathPlot 的文档被旧版本打开 → 未知类型静默忽略不崩溃 | L1 | 已覆盖→`mathplot-integration.test.ts` |
| F-09 | P2 | localStorage 禁用/配额满/键值被篡改为畸形 JSON → 加载侧优雅降级（空文档或可读错误），不白屏 | Man | 待补 |

## G. 现有白板功能回归（10 条 · 零回归硬指标）

前置：与迭代一基线（`main`@`36a528b` 行为）对照。

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| G-01 | P0 | 8 工具冒烟（数据驱动）：选择/画笔/矩形/圆/直线/箭头/文本/橡皮擦各画一笔 → 元素按预期创建/擦除，属性面板默认态正常 | L3 | 待补 |
| G-02 | P0 | 工具快捷键 V/B/R/C/L/A/T/E 原有映射不变；新增 F 不冲突 | L3 | 待补 |
| G-03 | P0 | 老元素选中/移动：与 mathPlot 共存时点选老元素 → 正常选中移动，面板显示老属性面板 | L3 | 待补 |
| G-04 | P0 | 老元素撤销/重做行为不变 | L3 | 待补 |
| G-05 | P0 | 视口平移（空格/拖拽）/滚轮缩放对老元素与新元素一致 | L3 | 待补 |
| G-06 | P1 | 属性面板默认态（无 equation 工具、无 mathPlot 选中）与迭代一视觉与行为一致（宽度 192px 基线） | L3/Man | 待补 |
| G-07 | P1 | 控点差异（D-1 零回归）：老元素仍 **4 角控点**且无拖拽缩放；mathPlot 为 8 控点——两者互不影响 | L1+L3 | 已覆盖→`mathplot-integration.test.ts`；L3 抽查待补 |
| G-08 | P1 | 历史面板对老操作显示/跳转不变 | L3 | 待补 |
| G-09 | P1 | 顶栏：新建/保存/导出/文档列表入口对新老元素混合文档可用 | L3 | 待补 |
| G-10 | P2 | 橡皮擦对 mathPlot 的行为边界：不误擦或明确不擦（按现有 hitTest 语义验证并记录现状） | L3/Man | 待补 |

## H. 性能与稳定性（8 条）

预算来源：技术方案 §6.4（PM 硬约束）。

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| H-01 | P0 | 单方程确认出图（parse+compile+640 采样+Path2D 构建）**<50ms**（中位/P95） | Perf | 待补（微基准脚本） |
| H-02 | P0 | 平移/缩放帧预算 **≤16ms（60fps）**：20 元素场景（mathPlot×20 @640 档最坏 12.8k 段 + 视口 culling） | Perf | 待补（Playwright 帧采样，R4 固化） |
| H-03 | P0 | **同屏 20 图形**流畅：创建/选中/调参/平移缩放无肉眼卡顿，帧率不塌陷 | Perf | 待补 |
| H-04 | P1 | 滑杆调参帧 <10ms：改颜色/线宽不重采样（缓存差分为 0）；改定义域/采样档单次重建 <10ms | Perf | 待补（`plotRenderWriteCount` 差分已有 L1 手法） |
| H-05 | P1 | 构建与体积：`next build` 通过；mathjs 引入的 gzip 增量记录在案（预算 +150KB 门禁，R1） | Perf/CI | 待补 |
| H-06 | P2 | 编辑器逐键校验延迟：连续键入 30 字符方程（编译缓存命中路径）无可感知卡顿 | Perf | 待补 |
| H-07 | P2 | 4K/高 DPR 大屏：渲染清晰、dpr 缩放正确、交互流畅 | Man | 待补（台架） |
| H-08 | P2 | 长会话内存：反复创建/删除/撤销 500 次 mathPlot → 无明显内存增长（WeakMap/LRU 回收生效） | Perf | 待补（heap 快照抽查） |

## I. 安全（5 条 · 禁 eval 硬约束）

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| I-01 | P0 | 注入载荷回归：`x);require('fs')`、`__proto__.x`、`constructor.constructor` 等 → parse/白名单阶段拒绝，不产生求值函数 | L1 | 已覆盖→`parse.test.ts` |
| I-02 | P0 | AST 节点白名单：Assignment/Accessor/Conditional/Block/Range 节点一律「无法识别的表达式」 | L1 | 已覆盖→`parse.test.ts` |
| I-03 | P0 | 求值 scope 只含 x：白名单外符号（y/t/a…）到不了解析后的求值 | L1 | 已覆盖→`parse.test.ts` |
| I-04 | P1 | 依赖供应链：锁 mathjs 版本 + CI `npm audit --omit=dev` 门禁（R5） | CI | 待补 |
| I-05 | P2 | 存储注入：localStorage 中文档 JSON 被篡改（畸形 kind/超长 equation/非法枚举值）→ 加载侧仅显示异常不执行任意代码 | Man | 待补 |

## J. 兼容性（8 条）

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| J-01 | P0 | Chrome（最新 ×2 大版本）：全部 P0 E2E 套件通过（基准浏览器） | L3 | 待补 |
| J-02 | P1 | Edge / Safari / Firefox：主链路冒烟（创建/调参/撤销/保存重开/导出）通过 | L3/Man | 待补 |
| J-03 | P1 | Safari 绘制兼容：Path2D stroke、手工圆角矩形（无 `roundRect` 依赖）、错误态虚线渲染正常 | Man | 待补（并入 J-02 抽查） |
| J-04 | P1 | 教学触屏一体机（1080p–4K，Chrome/Edge）：触摸选中/拖拽移动/控点缩放/面板触控可用 | Man | 待补（台架） |
| J-05 | P2 | 移动端（iOS Safari / Android Chrome）：只读浏览 + 平移缩放可用（PRD「不强求精细编辑」） | Man | 待补 |
| J-06 | P2 | 高 DPR（@2x）屏：曲线/刻度/文字清晰无锯齿模糊（canvas dpr 处理） | Man | 待补 |
| J-07 | P2 | 低端设备降级：大定义域 + 640 采样档仍可完成创建与交互（允许帧率下降但不冻结） | Man | 待补 |
| J-08 | P1 | **离线**：断网后全部功能（输入/出图/调参/保存/导出）可用，无外部资源请求失败 | Man | 待补 |

## K. 并发与竞态（5 条 · 语义界定见策略 §8）

| ID | P | 用例 | 分层 | 现状 |
|---|---|---|---|---|
| K-01 | P1 | 双标签页开同一白板：A 改动保存 → B 再保存 → 后写覆盖（last-write-wins 现状验收：不崩溃、不产生混合损坏数据） | Man/L3 | 待补 |
| K-02 | P2 | 双标签页 autosave 交错写入 → 文档 JSON 结构始终完整可解析 | Man | 待补 |
| K-03 | P1 | 快速操作风暴：连按回车连续插入 10 个元素、拖拽移动中 Ctrl+Z、缩放拖拽中 Del 删除 → 状态一致、无卡死、无悬空选中、历史栈完整 | L3 | 待补 |
| K-04 | P2 | 滑杆调参未松手时按 Ctrl+Z → 行为可预期（与现有移动拖拽中撤销的语义一致），不产生半提交状态 | L3/Man | 待补 |
| K-05 | P2 | autosave 写入瞬间刷新页面 → 重开文档为完整一致状态（无截断 JSON） | L3 | 待补 |

---

## 汇总与执行建议

| 组 | 条数 | P0/P1/P2 | 已有自动化 | 待补 |
|---|---|---|---|---|
| A 方程输入 | 14 | 6/6/2 | 0（A-11 半覆盖） | 14 |
| B 归一化解析 | 13 | 8/3/2 | 9 | 4 |
| C 采样渲染 | 14 | 7/5/2 | 10 | 4 |
| D 元素集成 | 20 | 10/8/2 | 5（L1 侧） | 15 |
| E 错误稳健 | 8 | 4/3/1 | 3（L1 侧） | 5 |
| F 持久化导出 | 9 | 2/4/3 | 2 | 7 |
| G 白板回归 | 10 | 5/4/1 | 1 | 9 |
| H 性能 | 8 | 3/2/3 | 0 | 8 |
| I 安全 | 5 | 3/1/1 | 3 | 2 |
| J 兼容 | 8 | 1/4/3 | 0 | 8 |
| K 并发竞态 | 5 | 0/2/3 | 0 | 5 |
| **合计** | **114** | **52/44/18** | **33** | **81** |

**执行节奏建议**：
1. **第一批（P0·L3+L2）**：D-01/06/07/08、E-01/02/03/07、G-01/02/05、A-03/04/05/06 —— 先点亮「创建→出图→错误→零回归」主路径，对应 PRD §10 验收 1–6；
2. **第二批（P0·Perf）**：H-01/02/03 压测脚本（R4 固化）；
3. **第三批（P1 矩阵）**：D-12 调参矩阵、F 组持久化、J-02 浏览器矩阵、K-01/03 竞态；
4. L1 增量（B-04/11、C-11/12、E-08）随批补入现有 vitest 套件；
5. 手工项（J-04/05、F-09、I-05）安排一轮集中台架/探索测试（建议 0.5–1 人日）。
